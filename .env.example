# Docker (VS / docker compose): po rebuild kontenera API jest na :5196 i :8080
MATDEV_API_BASE_URL=http://127.0.0.1:5196
NEXT_PUBLIC_MATDEV_API_BASE_URL=http://127.0.0.1:5196

# Aplikacja desktopowa (Electron) ustawia to automatycznie przy starcie.
# Zobacz DESKTOP.md — npm run build:desktop → instalator Windows.
